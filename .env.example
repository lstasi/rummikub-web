# Rummikub Web Configuration
# Copy this file to .env.local and set your values

# Admin password for creating games
# This password is required when creating new games via the admin API
RUMMIKUB_ADMIN_PASSWORD=your_secure_password_here

# Backend server URL (optional - can also be set in the UI)
RUMMIKUB_BACKEND_URL=http://localhost:8000

# Alternative configuration methods:
# 1. URL Parameter: ?password=your_password
# 2. Local Storage: Set once in the UI and it will be remembered
# 3. Environment Variable: Set RUMMIKUB_ADMIN_PASSWORD (this file)